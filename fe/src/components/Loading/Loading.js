import React from 'react';

/**
 * Component to Loading
*/
const Loading = () => {
  return (
    <h1>Loading</h1>
  );
};

Loading.propTypes = {
};

Loading.defaultProps = {
};

export default Loading;
