{
  "name": "server-beerstore",
  "version": "1.0.0",
  "description": "Server to provide endpoint to access db",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=prod node index.js",
    "lint": "eslint .",
    "test": "NODE_ENV=test jest --testTimeout=10000 --coverage"
  },
  "author": "Alexandre Dias <alexandrermd@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "eslint": "^6.8.0",
    "eslint-plugin-promise": "^4.2.1",
    "express": "^4.17.1"
  },
  "engines": {
    "node": "13"
  },
  "devDependencies": {
    "jest": "^25.3.0",
    "supertest": "^4.0.2"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
